# Copie para `.env.production` na raiz (mesma pasta que package.json).
# O Vite lê isto automaticamente em `npm run build`.
#
# URL pública da API Node (HTTPS, sem barra no final).
# Obrigatório quando o site está no Firebase e o backend está na VPS/outro domínio.
#
# Depois: na API, em ALLOWED_ORIGINS, inclua https://zapflow25.web.app (e o teu domínio se tiveres).

VITE_API_ORIGIN=https://SUBSTITUA_PELO_TEO_DOMINIO_DA_API
