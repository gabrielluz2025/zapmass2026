# Incluir uma vez na VPS (script setup-nginx-rate-limit.sh copia para /etc/nginx/conf.d/).
# Zona partilhada por todos os subdomínios de clientes Plano B.

limit_req_zone $binary_remote_addr zone=zapmass_client_api:20m rate=35r/s;
limit_req_status 429;
