/**
 * Bate-papo v2 — WhatsApp Web (visual + tempo real).
 * Implementação em `chat-v2/`. Versão anterior: `ChatTab.legacy.tsx`.
 */
export { WaWebChatApp as ChatTab, default } from './chat-v2/WaWebChatApp';
